#include <iostream>
#include <string>

int main()
{
	std::string stdString = "something";

	if (stdString.compare("something") == 0)
	{

	}

	if ("something" == stdString)
	{
		// Do stuff if strings are equal
	}
}
